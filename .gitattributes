# Shell scripts and Docker files must use LF so Linux containers can exec them.
# (CRLF shebangs fail with: exec ./docker-entrypoint.sh: no such file or directory)
*.sh text eol=lf
Dockerfile text eol=lf
docker-entrypoint.sh text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
